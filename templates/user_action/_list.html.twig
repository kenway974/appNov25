<div class="container">
    <div class="row">

        {% for userAction in userActions %}
            <div class="col-md-4 mb-4">
                {{ include('user_action/_show.html.twig', { userAction: userAction }) }}
            </div>
        {% endfor %}

    </div>
</div>