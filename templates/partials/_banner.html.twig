<section class="banner-container my-5" id="{{ id|default('banner') }}">

    <div class="banner-wrapper">
        <img src="{{ imageUrl }}" 
             alt="{{ alt|default('Bannière') }}" 
             class="img-fluid w-100 banner-image shadow-sm rounded">
    </div>

</section>