{% extends 'base.html.twig' %}

{% block title %}Dashboard - {{ user.username }}{% endblock %}

{% block body %}
    <div class="dashboard-container container">

        <!-- HEADER -->
        <div class="dashboard-block">
            {{ include('dashboard/partials/_header.html.twig') }}
        </div>

        <!-- ACTIONS -->
        <div class="dashboard-block">
            {{ include('dashboard/partials/_actions.html.twig') }}
        </div>

        <!-- BESOINS -->
        <div class="dashboard-block">
            {{ include('dashboard/partials/_needs.html.twig') }}
        </div>

    </div>
{% endblock %}