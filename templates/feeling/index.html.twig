{% extends 'base.html.twig' %}

{% block title %}Liste des émotions{% endblock %}

{% block body %}

<!-- Section Feelings -->
<div class="mt-5">
    <h2 class="text-center mb-4">Feelings par émotion</h2>


    {% if feelingsByEmotion is not empty %}
    <div id="carouselFeelingsByEmotion" class="carousel slide" data-bs-ride="carousel">

        <!-- Indicators --> 
        <div class="carousel-indicators">
            {% for emotion, feelings in feelingsByEmotion %}
                <button type="button"
                        data-bs-target="#carouselFeelingsByEmotion"
                        data-bs-slide-to="{{ loop.index0 }}"
                        class="{{ loop.first ? 'active' : '' }}"
                        {% if loop.first %}aria-current="true"{% endif %}
                        aria-label="Slide {{ loop.index }}">
                </button>
            {% endfor %}
        </div> 

        <!-- Slides -->
        <div class="carousel-inner">
            {% for emotion, feelings in feelingsByEmotion %}
                <div class="carousel-item {{ loop.first ? 'active' : '' }}" style="min-height: 250px;">
                    <h3 class="text-center mb-3">{{ emotion }}</h3>

                    {% if feelings is empty %}
                        <p class="text-center">Aucun besoin trouvé pour cette émotion.</p>
                    {% else %}
                        <div class="d-flex flex-column align-items-center gap-3">
                            {% for feeling in feelings %}
                                <div class="feeling-mobile">
    <div class="feeling-toggle-title"
         data-feeling-id="{{ feeling.id }}">
        <h5>{{ feeling.title }}</h5>
    </div>
    <div class="feeling-content d-none" id="contentFeeling{{ feeling.id }}">
        {{ include('feeling/_read.html.twig', { feeling: feeling }) }}
    </div>
</div>

<div class="feeling-desktop card" style="background-color: rgba(53, 53, 53, 0.05); border: none;">
    <div class="card-body">
        <h5 class="card-title text-center">{{ feeling.title }}</h5>
        <p class="card-text">
            {{ include('feeling/_read.html.twig', { feeling: feeling }) }}
        </p>
    </div>
</div>


                            {% endfor %}
                        </div>
                    {% endif %}
                </div>
            {% endfor %}
        </div>

        <!-- Controls -->
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselFeelingsByEmotion" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselFeelingsByEmotion" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
        </button>
    </div>
{% else %}
    <p class="text-center text-white">Aucun feeling trouvé.</p>
{% endif %}
        
</div>


{% endblock %}

{% block javascripts %}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            document.querySelectorAll('.user-need-title').forEach(function(el) {
                el.addEventListener('click', function() {

                    const id = this.dataset.id;

                    if (!id) {
                        console.warn('Aucun ID trouvé sur cet élément.');
                        return;
                    }

                    fetch('{{ path('set_selected_user_need') }}', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                            'X-Requested-With': 'XMLHttpRequest',
                            'X-CSRF-TOKEN': '{{ csrf_token('set_user_need') }}'
                        },
                        body: JSON.stringify({ id: id })
                    })
                    .then(res => {
                        if (!res.ok) {
                            throw new Error('Erreur serveur : ' + res.status);
                        }
                        return res.text();
                    })
                    .then(data => {
                        console.log('✅ ID enregistré en session :', data);
                    })
                    .catch(err => {
                        console.error('⚠️ Erreur lors de l’enregistrement :', err);
                    });
                });
            });
        });

        document.querySelectorAll('.feeling-toggle-title').forEach(function(title) {
        title.addEventListener('click', function() {
            const id = this.dataset.feelingId;
            const content = document.getElementById('contentFeeling' + id);
            if (content) {
                content.classList.toggle('d-none');
            }
        });
    });
    </script>


{% endblock %}
